const mysql = require('mysql');

const cfg = {
  host: 'localhost',
  user: 'zqh',
  password: 'zqh719719',
  database: 'test'
}

module.exports = {
  query: function (sql, value, callback) {
    
  }
}